import styled from 'styled-components';

const InputField = styled.div`
    position: relative;
    margin-bottom: 45px;
    textarea {
        min-height: 150px;
    }
    input[type='color'] {
        padding-left: 56px;
    }
`;

export default InputField;
